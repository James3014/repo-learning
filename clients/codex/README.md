# Codex projection

Canonical policy source: `skills/repo-learning/`.

Personal installation target: `${CODEX_HOME:-$HOME/.codex}/skills/repo-learning/`.

The installed directory must be a byte-for-byte projection of the canonical skill source; do not fork `SKILL.md` policy for Codex. Codex discovery/activation, state loading and an actual learning interaction are separate evidence claims.

A valid physical activation witness should show the `repo-learning` skill in the Codex session skill inventory or explicitly invoke `$repo-learning` from that installed path. Installation alone is not activation evidence.
